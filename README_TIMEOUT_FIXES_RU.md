# SkyCooker Integration - Исправления таймаутов и улучшения

## Обзор

Этот документ описывает улучшения, внесенные для исправления проблем с таймаутами в интеграции SkyCooker для Home Assistant для мультиварок Redmond RMC-M40S.

## Проблемы, выявленные в коде

### 1. Проблемы с таймаутами команд
- **Фиксированный таймаут 1.5с** был слишком коротким для некоторых операций
- **Нет адаптивного таймаута** в зависимости от типа команды
- **Нет механизма повторных попыток** для отдельных команд
- **Гонка условий** в обработке ответов с фиксированным polling 0.05с

### 2. Проблемы управления соединением
- **Нет валидации состояния соединения** перед отправкой команд
- **Несогласованная обработка ошибок** между уровнем соединения и команд
- **Нет мониторинга состояния соединения**

### 3. Проблемы реализации протокола
- **Отсутствующие константы команд** в const.py
- **Несогласованная обработка команд** между базовым классом и классом соединения
- **Нет очереди команд** для одновременных операций

## Реализованные решения

### 1. Адаптивная система таймаутов

Добавлены таймауты, зависящие от команды, в `const.py`:

```python
COMMAND_TIMEOUTS = {
    COMMAND_AUTH: 3.0,              # Аутентификация может быть медленной
    COMMAND_GET_VERSION: 5.0,       # Получение версии
    COMMAND_GET_STATUS: 2.0,        # Получение статуса
    COMMAND_SET_MAIN_MODE: 3.0,     # Установка режима
    COMMAND_TURN_ON: 2.0,           # Включение
    COMMAND_TURN_OFF: 2.0,          # Выключение
    COMMAND_SET_TEMPERATURE: 2.0,   # Установка температуры
    COMMAND_SET_COOKING_TIME: 2.0,  # Установка времени готовки
    COMMAND_SET_DELAY_TIME: 2.0,    # Установка отложенного старта
    COMMAND_SET_POST_HEAT: 2.0,     # Установка подогрева
    "default": 1.5                  # Стандартный таймаут
}
```

### 2. Улучшенная обработка команд

Метод `command()` теперь включает:
- **Адаптивные таймауты** в зависимости от типа команды
- **Автоматические повторные попытки** с экспоненциальной задержкой
- **Улучшенную обработку ошибок** с конкретными исключениями таймаута
- **Адаптивный polling** который увеличивает интервал со временем

### 3. Улучшенное управление соединением

Улучшенный метод `_connect_if_need()` с:
- **Лучшим логированием** состояний соединения
- **Более описательными сообщениями об ошибках**
- **Правильной обработкой аутентификации**
- **Мониторингом состояния соединения**

### 4. Функция тестирования соединения

Добавлена комплексная функция `test_connection()`, которая:
- **Тестирует все базовые команды** (статус, версия, управление питанием)
- **Проверяет аутентификацию**
- **Тестирует установку температуры и режимов**
- **Предоставляет детальное логирование** результатов
- **Возвращает структурированные результаты** для анализа

### 5. Интеграция сервиса Home Assistant

Регистрация сервиса для `skycooker.test_connection`:
- **Простой доступ** через Developer Tools Home Assistant
- **Трансляция событий** для результатов тестов
- **Удобное тестирование** во время разработки

## Использование

### Тестирование соединения (Разработка)

Для тестирования соединения и команд во время разработки:

1. **Через сервис Home Assistant**:
   ```yaml
   # В Developer Tools Home Assistant -> Services
   service: skycooker.test_connection
   ```

2. **Программно**:
   ```python
   # В вашем коде
   results = await cooker.test_connection()
   for result in results:
       print(f"{result['test']}: {result['status']} - {result['details']}")
   ```

### Настройка таймаутов команд

Вы можете настроить таймауты, изменив словарь `COMMAND_TIMEOUTS` в `const.py`:

```python
# Увеличить таймаут для конкретных команд
COMMAND_TIMEOUTS[COMMAND_GET_VERSION] = 10.0  # 10 секунд для проверки версии
```

### Ручное тестирование команд

Для ручного тестирования конкретных команд:

```python
# Тест с пользовательским таймаутом и повторными попытками
response = await cooker.command(
    COMMAND_GET_STATUS,
    timeout=3.0,  # Пользовательский таймаут
    retries=3     # Пользовательское количество повторных попыток
)
```

## Обработка ошибок

### Ошибки таймаута

- **TimeoutError**: Вызывается при превышении таймаута ответа команды
- **IOError**: Вызывается для недействительных ответов или проблем с соединением
- **AuthError**: Вызывается специально для ошибок аутентификации

### Логика повторных попыток

Команды автоматически повторяются с:
- **Экспоненциальной задержкой**: 0.2с, 0.4с, 0.6с задержки
- **Настройкой количества повторных попыток**: По умолчанию 2 повтора
- **Умными условиями повтора**: Только при таймаутах и определенных ошибках

## Логирование

Улучшенное логирование предоставляет подробную информацию:

```
🧪 Starting connection test
🔌 Testing connection...
✅ Connection: Connected successfully
🔑 Testing authentication...
✅ Authentication: Auth successful
📤 Testing command: Get Status (0x06)
✅ Command Get Status: Response: 0101003c00000000000000000000000000000000
🌡️ Testing temperature setting...
✅ Set Temperature: Temperature set to 60°C
⚙️ Testing mode switching...
✅ Set Mode: Mode set to Выпечка
```

## Устранение неполадок

### Распространенные проблемы

1. **Аутентификация не удалась**:
   - Убедитесь, что режим сопряжения включен на мультиварке
   - Проверьте правильность MAC-адреса
   - Убедитесь, что пароль/ключ правильный

2. **Таймаут соединения**:
   - Проверьте, что Bluetooth включен на хосте
   - Убедитесь, что мультиварка в пределах досягаемости
   - Попробуйте увеличить значения таймаута

3. **Таймаут команды**:
   - Некоторые команды естественно занимают больше времени (например, проверка версии)
   - Увеличьте таймаут для конкретных команд при необходимости
   - Проверьте уровень заряда батареи мультиварки

### Режим отладки

Включите отладочное логирование в конфигурации Home Assistant:

```yaml
logger:
  default: info
  logs:
    custom_components.skycooker: debug
```

## Улучшения производительности

### Адаптивный polling

Механизм ожидания ответа теперь:
- **Начинает с быстрого polling** (интервалы 50мс)
- **Увеличивает интервал со временем** (до 200мс)
- **Снижает использование CPU** во время длительных ожиданий

### Повторное использование соединения

- **Постоянные соединения** уменьшают накладные расходы на подключение
- **Умное повторное подключение** только при необходимости
- **Правильная очистка** неудачных соединений

## Обратная совместимость

Все изменения сохраняют обратную совместимость:
- **Существующие конфигурации** продолжают работать
- **Таймауты по умолчанию** остаются такими же для большинства команд
- **API остается неизменным** для внешних интеграций

## Тестирование

### Модульные тесты

Комплексные модульные тесты расположены в директории `tests/`:

```bash
# Запустить все тесты
python -m pytest tests/

# Запустить тесты, связанные с таймаутами
python -m pytest tests/test_timeout_fixes.py -v

# Запустить с покрытием
python -m pytest tests/ --cov=custom_components.skycooker
```

### Ручное тестирование

Для ручного тестирования конкретных команд:

```python
# Тест с пользовательским таймаутом и повторными попытками
response = await cooker.command(
    COMMAND_GET_STATUS,
    timeout=3.0,  # Пользовательский таймаут
    retries=3     # Пользовательское количество повторных попыток
)
```

## Будущие улучшения

Возможные направления для дальнейшего улучшения:
1. **Очередь команд** для одновременных операций
2. **Пул соединений** для нескольких устройств
3. **Расширенное восстановление после ошибок** с автоматическими fallback'ами
4. **Сбор метрик производительности** и их отчетность

## Ссылки

- [Английская версия документации](README_TIMEOUT_FIXES.md)